name: Fake IP Fixer
desc: >-
  此覆写可以过滤 fake-ip 并将其解析为真实 IP，请在 APP 的设置页面启用 "Fallback DNS" 以使用此功能。
  使用场景：当 WIFI 环境下的网关部署了 Clash 或其他代理时，网关解析的 IP 可能为 fake-ip(198.18.0.1/16)；如果切换到移动网络，Stash 可能无法及时更新 DNS，导致某些网站无法连接。

dns:
  fallback: #!replace
    - https://dns.alidns.com/dns-query

  fallback-filter: 
    geoip: false
    geoip-code: CN

    ipcidr:
      - '198.18.0.1/16'

rules:
  - DOMAIN,dns.alidns.com,DIRECT
